script: true
notifications:
  email: false
conditions: v1
env:
  - FOO=1
  - FOO=2
jobs:
  include:
    - stage: Yo!
      script: echo stages
      # on v0 this would produce a parse error, which currently returns `true`, and thus passes
      if: branch NOT IN (sf-stages-passing)
